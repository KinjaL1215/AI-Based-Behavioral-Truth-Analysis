services:
  - type: web
    name: truth-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    aptPackages:
      - libgl1
      - ffmpeg